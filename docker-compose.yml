version: '3.8'

services:
  mt5:
    container_name: mt5
    build:
      context: .
      dockerfile: Dockerfile.mt5
    ports:
      - "8001:8001"
    environment:
      - MT5_LOGIN=${MT5_LOGIN}
      - MT5_PASSWORD=${MT5_PASSWORD}
      - MT5_SERVER=${MT5_SERVER}
    restart: unless-stopped

  bot:
    container_name: bot
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - mt5
    environment:
      - MT5_LOGIN=${MT5_LOGIN}
      - MT5_PASSWORD=${MT5_PASSWORD}
      - MT5_SERVER=${MT5_SERVER}
      - MONGODB_URI=${MONGODB_URI}
    command: python3 trading_bot/bot.py
