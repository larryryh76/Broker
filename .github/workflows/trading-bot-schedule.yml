name: The Money Machine Trading Bot

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: # Allows manual triggering

jobs:
  trade:
    runs-on: windows-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Configure Git for Windows
        shell: bash
        run: |
          git config --global core.longpaths true
          git config --global core.fsmonitorwatchmanfolder true

      - name: Set up Python 3.12
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Setup Headless Display (Mesa3D)
        uses: pyvista/setup-headless-display-action@v3
        with:
          mesa3d-release: '24.3.0'

      - name: Force-Kill Ghost Processes
        shell: cmd
        continue-on-error: true
        run: taskkill /F /IM terminal64.exe /T /FI "STATUS eq RUNNING" 2>NUL & taskkill /F /IM setup.exe /T /FI "STATUS eq RUNNING" 2>NUL & exit 0

      - name: Install Dependencies
        shell: cmd
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Download Verified 64-bit MT5
        shell: powershell
        run: |
          $dir = "$env:GITHUB_WORKSPACE\mt5_terminal"
          if (!(Test-Path $dir)) { New-Item -ItemType Directory -Force -Path $dir }
          # Use the direct FBS 64-bit link
          $url = "https://download.mql5.com/cdn/web/fbs.markets.inc/mt5/fbs5setup.exe"
          Invoke-WebRequest -Uri $url -OutFile "$dir\setup.exe"
          # Extract the terminal64.exe specifically from the setup
          Start-Process -FilePath "$dir\setup.exe" -ArgumentList "/auto", "/quit", "/portable", "/path:$dir" -Wait
          # Verify file size (should be > 10MB)
          if (Test-Path "$dir\terminal64.exe") {
            $file = Get-Item "$dir\terminal64.exe"
            if ($file.Length -lt 1000000) { throw "File is too small/corrupted!" }
            Write-Host "Verified 64-bit Terminal Found: $($file.Length) bytes"
          } else {
            throw "terminal64.exe not found after extraction!"
          }

      - name: Pre-Execution Environment Prep
        shell: powershell
        run: |
          $dir = "$env:GITHUB_WORKSPACE\mt5_terminal\MQL5\Profiles\Charts\Default"
          if (!(Test-Path $dir)) { New-Item -ItemType Directory -Force -Path $dir }
          New-Item -ItemType File -Force -Path "$dir\default.tpl"
          Write-Host "Created empty default.tpl at $dir"

          # Pre-seed MT5 configuration to bypass login dialogs
          $cfgDir = "$env:GITHUB_WORKSPACE\mt5_terminal\config"
          if (!(Test-Path $cfgDir)) { New-Item -ItemType Directory -Force -Path $cfgDir }
          $iniPath = "$cfgDir\terminal.ini"
          @"
          [Common]
          Login=${{ secrets.MT5_LOGIN }}
          Password=${{ secrets.MT5_PASSWORD }}
          Server=${{ secrets.MT5_SERVER }}
          KeepPrivate=1
          EnableNews=0
          CertInstall=1

          [Charts]
          MaxBars=5000
          "@ | Out-File -Encoding ASCII $iniPath
          Write-Host "Injected terminal.ini configuration."

      - name: Run Trading Bot
        shell: cmd
        env:
          MT5_LOGIN: ${{ secrets.MT5_LOGIN }}
          MT5_PASSWORD: ${{ secrets.MT5_PASSWORD }}
          MT5_SERVER: ${{ secrets.MT5_SERVER }}
          MONGODB_URI: ${{ secrets.MONGODB_URI }}
        run: python main.py
